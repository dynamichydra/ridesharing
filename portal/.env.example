# Copy to .env (local dev), .env.staging, or .env.production and set the real backend URL.
# Must include a trailing slash. Falls back to http://localhost:3000/ if unset.
VITE_API_BASE_URL=http://localhost:3000/
